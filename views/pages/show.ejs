<!DOCTYPE html>
<html>


<head>
    <title>
        show
    </title>
    <link rel="stylesheet" href="styles/reset.css">
    <link rel="stylesheet" href="styles/base.css">
    <link rel="stylesheet" href="styles/layout.css">
    <link rel="stylesheet" href="styles/modules.css">

</head>

<body>
    <section>

        <h1>Best Books!</h1>
        <div>
            <h2>Search Results</h2>
            
            <% books.forEach((book)=> { %>
                <div id="gray">
                <ul>

                        <li><img src=<%= book.volumeInfo.imageLinks.thumbnail %></li> 
                   
                    <li id="title"><%= book.volumeInfo.title %></li>
                   
                    <li id="author"><%= book.volumeInfo.authors %></li>
                  
                    <li id="desc"><%= book.volumeInfo.description %></li>
                    <li ><%= book.volumeInfo.industryIdentifiers[0].type %></li>
                   
                    <% }); %> </ul> </div> </section>
                            </body> </html>